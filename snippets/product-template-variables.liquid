{%- comment -%}
  Product FAQ JSON-LD wrapper.

  The Product structured data that Yoast SEO previously emitted here was
  consolidated into snippets/product-schema.liquid (SBS-292) to remove the
  duplicate Product node from product pages. This snippet now only renders
  the FAQPage schema.
{%- endcomment -%}
{%- render 'product-faq-schema' -%}
