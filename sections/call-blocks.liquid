{% content_for 'block', type: 'call-actions', id: 'call-actions' %}

{% schema %}
{
  "name": "Call blocks",
  "blocks": [{ "type": "@theme" }],
  "settings": [],
  "presets": [
    {
      "name": "Call blocks",
      "blocks": [
        {
          "type": "call-actions",
          "static": true,
          "id": "call-actions",
        },
      ]
    }
  ]
}
{% endschema %}
