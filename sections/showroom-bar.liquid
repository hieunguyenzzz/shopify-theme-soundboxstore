{% schema %}
  {
    "name": "Showroom bar",
 "blocks": [
      {
        "type": "showroom",
        "name": "showroom",
        "settings": [
             {
            "type": "text",
            "id": "heading",
            "label": "Heading"
          },
            {
            "type": "text",
            "id": "subheading",
            "label": "Sub Heading"
          },
          {
            "type": "url",
            "id": "link",
            "label": "Url"
          }
        
        ]
      }
    ]
  }
{% endschema %}

